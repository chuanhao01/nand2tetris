## Notes?

So the jack2vm parser takes the class name from the class dec
So u could name the file and class differently.

Weird bug where the heap keeps changing values in the final while loop?

## Where did I stop

I have managed to code the entire thing till project 12
Implementing the OS is nice, but working on graphics is not my main goal
Although the rust compiler has a few quirks that are different from the proposed nand2tetris compiler, to me it is considered done.
