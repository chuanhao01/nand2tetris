class Main {
    function void main() {
        var Array r;          // stores test results;

        let r = 8000;

        let temp = 100 / 20;
        do Memory.poke(8003, temp);  // RAM[8003] = 5
        do Memory.poke(8004, 10);

        let temp = Math.sqrt(100);
        do Memory.poke(8005, temp);  // RAM[8003] = 10
        return;
    }

}
